# Hooks (full text)

The block below is included verbatim from `hooks/*.sh` in this
repository (glob, alphabetical). Each hook's header comments document
its trigger, mode behaviour, and block strategy. Edits go to the
primitive, not this page.

```bash
{%
  include "../../../hooks/*.sh"
%}
```
