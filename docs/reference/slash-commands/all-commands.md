# Slash commands (full text)

Each section below is included verbatim from a `commands/<name>.md`
file in this repository. Edits go to the primitive, not this page.

{%
  include-markdown "../../../commands/*.md"
  heading-offset=1
%}
